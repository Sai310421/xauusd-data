name: GoldeBrave BP-D Pressure Rescue V2 Raw
on:
  push:
    paths:
      - 'research/goldebrave_bpd_pressure_rescue_v2_raw.py'
      - '.github/workflows/goldebrave-bpd-pressure-rescue-v2-raw.yml'
  workflow_dispatch:
jobs:
  bt:
    strategy:
      fail-fast: false
      matrix:
        gate: [55, 60, 65]
    runs-on: ubuntu-24.04
    env:
      NAUTILUS_VERSION: '1.230.0'
      RAW_CATALOG: catalog/raw_bidask
      RAW_CACHE_KEY: raw-bidask-duka-6sym-2026-07-27-30d-v2
      EXPERIMENT_ID: gb-bpd-v2-${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install "nautilus_trader==${NAUTILUS_VERSION}" numpy pandas
      - uses: actions/cache/restore@v4
        with:
          path: ${{ env.RAW_CATALOG }}
          key: ${{ env.RAW_CACHE_KEY }}
          fail-on-cache-miss: true
      - name: Run BP-D v2 Raw
        run: |
          python research/goldebrave_bpd_pressure_rescue_v2_raw.py \
            --catalog "$RAW_CATALOG" \
            --gate "${{ matrix.gate }}" \
            --experiment-id "$EXPERIMENT_ID"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.EXPERIMENT_ID }}-g${{ matrix.gate }}
          path: results/goldebrave-bpd-pressure-rescue-v2/${{ env.EXPERIMENT_ID }}/*.json
          retention-days: 90
