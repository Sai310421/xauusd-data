name: ARCHIVED - MinimumSpike MTF OHLC Proxy (BLOCKED)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-24.04
    steps:
      - name: OHLC resample BT prohibited
        run: |
          echo 'BLOCKED: OHLC-resample-based backtests are prohibited for canonical Nautilus BT.'
          echo 'Use .github/workflows/nautilus-6symbol-3tf-raw-bidask.yml instead.'
          echo 'Policy: docs/RAW_BIDASK_ONLY_BT_POLICY.md'
          exit 1
