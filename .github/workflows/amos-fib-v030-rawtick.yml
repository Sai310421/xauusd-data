name: AMOS Fib v0.30 RawTick Validation
on:
  workflow_dispatch:
  push:
    paths:
      - 'research/amos_fib_v030_rawtick_parity.py'
      - '.github/workflows/amos-fib-v030-rawtick.yml'
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-24.04
    env:
      NAUTILUS_VERSION: 1.230.0
      RAW_CATALOG: catalog/raw_bidask
      RAW_CACHE_KEY: raw-bidask-duka-6sym-2026-07-27-30d-v2
      EXPERIMENT_ID: amos-fib-v030-rawtick-${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install pinned NautilusTrader
        run: |
          python -m pip install --upgrade pip
          python -m pip install "nautilus_trader==${NAUTILUS_VERSION}" numpy
      - name: Restore Raw BidAsk catalog cache
        uses: actions/cache/restore@v4
        with:
          path: catalog/raw_bidask
          key: ${{ env.RAW_CACHE_KEY }}
          fail-on-cache-miss: true
      - name: Guard Raw XAUUSD catalog
        run: python research/raw_bidask_catalog_guard.py --catalog "$RAW_CATALOG" --symbols XAUUSD
      - name: Run AMOS Fib source-parity validation
        run: |
          PYTHONPATH=research python research/amos_fib_v030_rawtick_parity.py \
            --catalog "$RAW_CATALOG" \
            --experiment-id "$EXPERIMENT_ID" \
            --raw-bidask-only
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: amos-fib-v030-rawtick-${{ github.run_id }}
          path: results/amos-fib-v030-rawtick/${{ env.EXPERIMENT_ID }}/*.json
          retention-days: 90
