# Adaptive v2.1 Nautilus BT Gate

This gate validates the Strategy-Specific Primary + Adaptive E1/E2/E3 execution architecture using repository raw Bid/Ask quotes only.

It intentionally refuses to fabricate metrics when raw quote data is unavailable and does not use OHLC resampling.
