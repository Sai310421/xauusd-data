# SEPARATION

## DexG Lab

Owns: DAIGO-second Entry/TP v1.0 baseline, X price-state observations, DexG hypotheses, DexG Raw Tick BT, DexG ablation, DexG adopted EDGE.

## Not in this window

- G75 as a DexG feature / ARMED gate / X_t
- G75 TSUGI, Negative Memory, Hydra66-as-G75-input, EV-G75
- any file under research/ whose name starts with g75-

G75 still exists elsewhere as `G75_PURSUIT` in `Sai310421/my-ea-factory-ICT`:
attach only when a non-DexG base EA already has WR >= 65%.
That success is not DexG evidence.

No shared Baseline. No shared feature vector. No shared promotion ledger.
If a DexG result cites G75, it is invalid as DexG evidence.
